# ADR-013 (DRAFT) — voice_dial blocked by VR-G5

> **Status: DRAFT — requires operator decision before this ADR can be
> moved to `08_decisions_log.md`. Action remains blocked until then.**

**Date:** 2026-06-05 (auto, UTC)
**Triggered by:** `voice.voice_dial` proposing `voice_dial`
**Correlation ID:** `(none)`
**Violated rule:** `VR-G5`
**Reason:** ADR-002 forbids autonomous dialing; voice leg produces voicemail drafts only.

## Proposed action (raw)

```json
{
  "campaign": "",
  "phone": "",
  "prospect_id": "p2"
}
```

## Decision required

Choose ONE:

- **A — ALLOW (with constraints):** Author a real ADR in
  `docs/codex/08_decisions_log.md` explaining why this action is
  permitted, what new gates accompany it, and what failure modes it
  introduces. Reference any prior ADRs it supersedes. Then move this
  draft to `_resolved/` and commit.
- **B — REJECT:** Modify the code so this action no longer triggers the
  violation. Delete this draft file.

## Why this draft exists

The Codex Validation Layer (chapter 12 / ADR-011) caught this proposed
action under rule `VR-G5` and refuses to let it run until a
real decision is recorded. This is the discipline that keeps the system
honest when nobody is watching.

— autogenerated by `backend/common/codex/adr_drafter.py`

## Resolution

**Decision:** ALLOW
**Operator:** alex
**Resolved at:** 2026-06-24T00:00:00+00:00
**Rationale:** ALLOW IN PRINCIPLE, OPERATIONALLY GATED — promoted to ADR-014 in 08_decisions_log.md (provisional draft number 013 renumbered to 014 to avoid collision with the outreach_send promotion). voice_dial is accepted as a future capability, but activation is DEFERRED and does NOT turn on now: it is gated on (1) Twilio compliance clearance and (2) phone-number purchase/provisioning (vapi_phone_number_id), both currently unmet. Until both clear, the voice leg stays voicemail-draft / limited-Vapi only and autonomous dialing stays DISABLED. VR-G5 remains blocking and unconditional; no code or rule change. Lifting the deferral will require a new successor ADR. See ADR-014 for full consequences and failure modes.
