# ADR-013 (DRAFT) — outreach_send blocked by VR-G8

> **Status: DRAFT — requires operator decision before this ADR can be
> moved to `08_decisions_log.md`. Action remains blocked until then.**

**Date:** 2026-06-23 (auto, UTC)
**Triggered by:** `outreach.run_campaign` proposing `outreach_send`
**Correlation ID:** `(none)`
**Violated rule:** `VR-G8`
**Reason:** Guardrail G8: outreach refuses to fire on a prospect with no warmth signal (RFP, chamber, prior inbound, public registry).

## Proposed action (raw)

```json
{
  "stake_sentence": "Saw the Marysville City Council minutes naming your shop for the levee-district contract — wanted you to see this."
}
```

## Decision required

Choose ONE:

- **A — ALLOW (with constraints):** Author a real ADR in
  `docs/codex/08_decisions_log.md` explaining why this action is
  permitted, what new gates accompany it, and what failure modes it
  introduces. Reference any prior ADRs it supersedes. Then move this
  draft to `_resolved/` and commit.
- **B — REJECT:** Modify the code so this action no longer triggers the
  violation. Delete this draft file.

## Why this draft exists

The Codex Validation Layer (chapter 12 / ADR-011) caught this proposed
action under rule `VR-G8` and refuses to let it run until a
real decision is recorded. This is the discipline that keeps the system
honest when nobody is watching.

— autogenerated by `backend/common/codex/adr_drafter.py`

## Resolution

**Decision:** ALLOW
**Operator:** alex
**Resolved at:** 2026-06-24T00:00:00+00:00
**Rationale:** ALLOW with constraints — promoted to ADR-013 in 08_decisions_log.md. The allowance is bound to VR-G8's own invariant: outreach_send is permitted only for a payload carrying a non-empty legitimacy_signal (warmth signal), and remains blocked without it. VR-G8 stays blocking and unchanged. Accompanying safeguard: the cold-send email-quality gate (is_cold_sendable_email in backend/prospecting/contact_validation.py, consulted by backend/outreach/cash_engine_entry.py) now fail-closes non-deliverable and role/system mailboxes ahead of the G8-cleared compose path, protecting sender-domain reputation. See ADR-013 for full consequences and failure modes.
