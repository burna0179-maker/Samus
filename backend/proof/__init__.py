"""Proof assets — case studies + social-proof aggregation (Phase F).

AI answer engines weight third-party validation when synthesizing
recommendations, so structured, schema-marked proof is both a close asset and
an AI-citation magnet. This package turns a closed-won outcome into a
structured case study (Challenge -> Tried -> Used -> Result -> Quote) with
Article JSON-LD, and aggregates proof points into a "wall of love" feed.

Pure + fail-closed: narrative generation is LLM-optional and always degrades to
a deterministic assembly of the structured fields.
"""
