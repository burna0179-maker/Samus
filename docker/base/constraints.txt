# Major-version pins for the Samus dependency surface.
# Concrete versions still come from Samus/requirements.lock; this file is the
# major-version ceiling that guards against a silent jump to the next major
# (CWE-1357 — a rebuild must not float a dependency to an unvetted release).
#
# Lower bounds are set to the version range proven to satisfy the codebase's
# imports; upper bounds exclude the next major. Where the exact pinned version
# lives in requirements.lock the lower bound is kept at-or-below it so the lock
# always validates against this file.
#
# Every dependency declared in Samus/requirements.txt has an entry here.
# If a package below is unpinnable with confidence it carries a `# TODO: pin`.

# --- Core web + validation stack ---
pydantic>=2.13,<3
pydantic-settings>=2.14,<3
fastapi>=0.118,<1
uvicorn>=0.30,<1
httpx>=0.27,<1
jinja2>=3.1.4,<4

# --- AWS SDK ---
boto3>=1.34,<2
botocore>=1.34,<2

# --- Observability / config ---
prometheus_client>=0.19,<1
python-dotenv>=1.0,<2
structlog>=24.1,<26

# --- Resilience ---
tenacity>=8.2,<10

# --- Graph driver ---
neo4j>=5.14,<7

# --- Crypto (CVE history — keep the ceiling tight, floor at a recent stable) ---
cryptography>=42.0,<49

# --- Serialization (CVE history — 6.x is the current stable major) ---
PyYAML>=6.0,<7

# --- HTML / scraping ---
beautifulsoup4>=4.12,<5

# --- DNS resolver ---
dnspython>=2.4,<3

# --- IANA tz database (CalVer-versioned: YYYY.N; bound the year band) ---
tzdata>=2024.1,<2027

# --- YouTube transcript fetcher (0.x — fast-moving; cap the minor band) ---
youtube-transcript-api>=0.6,<2

# --- ngrok tunnel for the voice workcell (1.x stable major) ---
ngrok>=1.0,<2
